version: "3.7"
services:
    app:
        image: ip_rejector_app:${application_tag}
        build:
            context: application
            args:
                PythonVersion: ${python_version}
        volumes:
            - "./application/:${container_mountpoint_application}"
            - "./test/:/var/test"
        stdin_open: true
        tty: true
